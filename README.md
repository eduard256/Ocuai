# Ocuai - AI-Powered Video Surveillance System

**Ocuai** — это современная система видеонаблюдения с поддержкой ИИ, универсальной интеграцией IP-камер, веб-интерфейсом и интеграцией с Telegram. Теперь с полноценной системой авторизации и поддержкой ВСЕХ типов камер!

## 🔐 Система Авторизации

### Первый запуск
При первом запуске Ocuai вы увидите страницу **настройки администратора**:
1. Введите желаемое имя пользователя и пароль (минимум 6 символов)
2. Нажмите "Create Administrator Account"
3. После создания аккаунта вы будете перенаправлены на страницу входа

### Вход в систему
- Перейдите по адресу `http://localhost:8080`
- Введите логин и пароль администратора
- После успешного входа откроется панель управления

### Безопасность
- ✅ Cookie-based сессии с защитой от CSRF
- ✅ Хэшированные пароли (bcrypt)
- ✅ Защищены все API endpoints кроме `/api/auth/*`
- ✅ HttpOnly cookies для предотвращения XSS
- ✅ Автоматическое завершение сессий

## 📷 Универсальная Поддержка Камер

### Возможности
- **Автоматическое обнаружение** - сканирует и находит ВСЕ доступные потоки камеры
- **Поддержка старых камер** - работает даже с самыми древними моделями
- **Умный выбор потока** - рекомендует лучший поток для использования
- **Без конфигурации** - не нужно знать RTSP URL или спецификации камеры
- **WebRTC стриминг** - низкая задержка прямо в браузере

### Поддерживаемые протоколы
- RTSP/RTSPS (все варианты)
- ONVIF
- HTTP/MJPEG/Snapshots
- TP-Link Tapo
- DVR-IP/XMeye
- Hikvision ISAPI
- Bubble/ESeeCloud
- И многие другие!

### Как добавить камеру
1. Нажмите "Add Camera" на странице камер
2. Введите IP адрес камеры
3. Введите логин/пароль (или оставьте пустым)
4. Система автоматически найдет все доступные потоки
5. Выберите нужный поток и добавьте камеру

[Подробная документация по камерам](CAMERA_INTEGRATION.md)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Go модули
go mod tidy

# Frontend зависимости (если планируете изменять)
cd web && npm install
```

### 2. Запуск приложения

```bash
# Компиляция
go build -o ./bin/ocuai ./cmd/ocuai

# Запуск
./bin/ocuai

# Или с настройкой порта
./bin/ocuai -port 8080
```

### 3. Первоначальная настройка

1. Откройте браузер и перейдите на `http://localhost:8080`
2. **Важно**: При первом запуске будет показана страница регистрации администратора
3. Создайте аккаунт администратора
4. После создания войдите в систему

## 📁 Структура проекта

```
Ocuai/
├── cmd/ocuai/           # Точка входа приложения
├── internal/
│   ├── auth/            # 🔐 Система авторизации
│   │   ├── auth.go      # Основная логика авторизации
│   │   ├── context.go   # Контекстные хелперы
│   │   └── handlers.go  # HTTP обработчики
│   ├── go2rtc/          # 📷 Интеграция go2rtc (НОВОЕ!)
│   │   ├── go2rtc.go    # Управление процессом go2rtc
│   │   └── scanner.go   # Сканер камер
│   ├── config/          # Конфигурация
│   ├── storage/         # База данных SQLite
│   ├── web/             # Веб-сервер и API
│   ├── ai/              # ИИ обработка
│   ├── streaming/       # Обработка видеопотоков
│   ├── events/          # Система событий
│   └── telegram/        # Telegram бот
├── web/                 # Frontend (Svelte + Tailwind)
│   ├── src/
│   │   ├── components/
│   │   │   └── Auth/    # 🔐 Компоненты авторизации (НОВОЕ!)
│   │   ├── stores/
│   │   │   └── auth.js  # 🔐 Store авторизации (НОВОЕ!)
│   │   └── utils/
│   │       └── api.js   # 🔐 API клиент (ОБНОВЛЕН!)
│   └── ...
├── data/                # Данные приложения
│   ├── ocuai.db         # SQLite база (включая таблицу users)
│   └── videos/          # Записанные видео
└── scripts/             # Скрипты развертывания
```

## 🔧 Конфигурация

Конфигурация находится в `~/.ocuai/config.yaml`:

```yaml
server:
  host: "0.0.0.0"
  port: "8080"

storage:
  database_path: "~/.ocuai/ocuai.db"
  video_path: "~/.ocuai/videos"
  retention_days: 7
  max_video_size_mb: 50

security:  # 🔐 НОВАЯ СЕКЦИЯ!
  session_secret: ""  # Генерируется автоматически

telegram:
  token: ""
  allowed_users: []
  notification_hours: "08:00-22:00"

ai:
  enabled: false
  model_path: "~/.ocuai/models/yolov8n.onnx"
  threshold: 0.5
  device_type: "cpu"
```

## 🔒 API Endpoints

### Публичные (не требуют авторизации):
- `GET /api/auth/setup` - Проверка необходимости первоначальной настройки
- `POST /api/auth/register` - Регистрация администратора (только при первом запуске)
- `POST /api/auth/login` - Вход в систему
- `POST /api/auth/logout` - Выход из системы
- `GET /api/auth/status` - Статус авторизации

### Защищенные (требуют авторизации):
- `GET /api/health` - Проверка здоровья системы
- `GET /api/stats` - Статистика системы
- `GET /api/cameras` - Список камер
- `POST /api/cameras` - Добавление камеры
- `GET /api/events` - События
- И все остальные API...

## 🛡️ Функции безопасности

### Реализованные меры защиты:
1. **Хэшированные пароли** - используется bcrypt с соленым хэшем
2. **Защищенные сессии** - HttpOnly cookies с SameSite защитой
3. **Middleware авторизации** - все API защищены кроме auth endpoints
4. **Защита от CSRF** - SameSite=Lax cookies
5. **Автоматическое завершение сессий** - настраиваемое время жизни
6. **Проверка ролей** - готовность к расширению системы ролей

### Настройки безопасности:
- Время жизни сессии: **7 дней** (настраивается)
- Минимальная длина пароля: **6 символов**
- Только один администратор может быть зарегистрирован

## 🌟 Новые возможности

### Камеры и Стриминг:
- ✅ **Универсальный сканер камер** - автоматически находит все потоки
- ✅ **Поддержка ВСЕХ типов камер** - от современных до самых старых
- ✅ **WebRTC стриминг** - низкая задержка в браузере
- ✅ **Интеграция go2rtc** - профессиональный стриминг сервер
- ✅ **Умный выбор потока** - рекомендации лучшего качества
- ✅ **Красивый видеоплеер** - с контролами и полноэкранным режимом

### Веб-интерфейс:
- ✅ Красивые страницы входа и регистрации
- ✅ Адаптивный дизайн
- ✅ Индикатор загрузки
- ✅ Обработка ошибок
- ✅ Информация о пользователе в header
- ✅ Кнопка выхода из системы

### Backend:
- ✅ Полноценная система авторизации
- ✅ Защита всех API endpoints
- ✅ Система сессий на cookies
- ✅ Middleware для проверки авторизации
- ✅ Готовность к масштабированию

## 🚀 Deployment

### Development:
```bash
# Запуск в режиме разработки
go run cmd/ocuai/main.go

# Или с дополнительными параметрами
go run cmd/ocuai/main.go -port 8080 -config ./config.yaml
```

### Production:
```bash
# Сборка
go build -ldflags="-s -w" -o ocuai cmd/ocuai/main.go

# Создание systemd сервиса
sudo cp scripts/ocuai.service /etc/systemd/system/
sudo systemctl enable ocuai
sudo systemctl start ocuai
```

## 📝 Использование

1. **Первый запуск**: Создайте администратора
2. **Добавьте камеры**: Через веб-интерфейс в разделе "Камеры"
3. **Настройте ИИ**: Включите обнаружение объектов (опционально)
4. **Настройте Telegram**: Добавьте токен бота для уведомлений
5. **Мониторинг**: Следите за событиями в реальном времени

## 🔍 Поиск проблем

### Проблемы с авторизацией:
- **Не могу войти**: Проверьте правильность логина/пароля
- **Сессия истекла**: Повторно войдите в систему
- **Ошибка 401**: API endpoint требует авторизации

### Первоначальная настройка:
- **Не показывается страница регистрации**: Проверьте, что база данных пуста
- **Ошибка при создании пользователя**: Проверьте права доступа к базе данных

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. файл [LICENSE](LICENSE) для деталей.

---

**⚡ Теперь ваш Ocuai полностью защищен и готов к production использованию!**